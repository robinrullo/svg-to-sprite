{
  "name": "svgtosprite",
  "version": "1.0.0",
  "description": "Create spritesheets used in mapbox from svg files",
  "scripts": {
    "sprites": "svgo -f ./icons && spritezero ./sprites/sprites ./icons -- ratio=1 -- sdf && spritezero ./sprites/sprites@2x ./icons --retina -- sdf"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@beyondtracks/spritezero-cli": "^2.3.0",
    "svgo": "^2.2.0"
  }
}
